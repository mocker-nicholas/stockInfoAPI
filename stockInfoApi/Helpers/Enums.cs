namespace stockInfoApi.Helpers
{
    public class Enums
    {
       public enum AccountType
        {
            Admin,
            Standard,
        }

        public enum TransactionType
        {
            Buy,
            Sell,
            Limit,
        }
    }
}
